extend ../layout2
block content
  // Content Header (Page header)
  section.content-header
    h1
      | Permissions Management
  // Main content
  section.content
    .row
      // left column
      .col-md-12
        // general form elements
        .box.box-primary
          .box-header
            if action == '/etbs-permissions/insert'
              h3.box-title New
            else if action == '/etbs-permissions/update'
              h3.box-title Edit
            else
              h3.box-title Remove
          // /.box-header
          // form start
          form(method='POST' action=action)
            .box-body
              if permission != undefined
                input#originPermission(type='hidden' name='originPermission' value=permission)
              if profileid != undefined
                input#originProfileid(type='hidden' name='originProfileid' value=profileid)
              if perm_type != undefined
                input#originPerm_type(type='hidden' name='originPerm_type' value=perm_type)
              .form-group
                label(for='permission') Permission
                input#permission.form-control(type='text', placeholder='Permission' name='permission' value=permission required)
              .form-group
                label(for='profileid') Profile ID
                input#profileid.form-control(type='text', placeholder='Profile ID' name='profileid' value=profileid required)
              .form-group
                label(for='perm_type') Permission Type
                input#perm_type.form-control(type='text', placeholder='Permission Type' name='perm_type' value=perm_type required)
              .form-group
                label(for='is_active') Active
                input#is_active.form-control(type='text', placeholder='Active' name='is_active' value=is_active)
              if rolename != undefined
                .form-group
                  label(for='rolename') Role
                  p
                    a(href= '/etbs-permissions/roles/' + permission + '/' + profileid + '/' + perm_type)= rolename
              // /.box-body
              .box-footer
              if action == '/etbs-permissions/insert'
                button.btn.btn-primary(type='submit') Create
              else if action == '/etbs-permissions/update'
                button.btn.btn-primary(type='submit') Save
              else
                button.btn.btn-danger(type='submit') Remove
